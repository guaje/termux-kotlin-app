# DISABLED: This workflow has been retired in v2.5.0.
# Release automation now lives entirely in release.yml, which builds and validates
# signed release APKs only. Debug APKs are never attached to releases.
#
# Kept as a tombstone to prevent accidental restoration by merge.
name: Disabled Attach Debug APKs
on:
  workflow_dispatch:
jobs:
  no-op:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled."
